version: "3"

services:

  app:
    container_name: db
    image: postgres:14.7-alpine
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: lab
    ports:
      - "5432:5432"